<%= form_for(@wfeat) do |f| %>
  <% if @wfeat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wfeat.errors.count, "error") %> prohibited this wfeat from being saved:</h2>

      <ul>
      <% @wfeat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
